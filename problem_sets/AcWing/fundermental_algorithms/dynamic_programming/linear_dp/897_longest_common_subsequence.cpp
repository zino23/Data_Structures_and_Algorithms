#include <iostream>

using namespace std;

const int N = 1e3 + 10;
int n, m;
int a[N], b[N];
int f[N][N];

int main()
{
	cin >> n >> m;
	for ( int i = 1; i <= n; i ++ ) cin >> a[i];
	for ( int i = 1; i <= n; i ++ ) cin >> b[i];

	for ( int i = 1; i <= n; i ++ )
	{
		for ( int j = 1; j <= m; j ++ )
		{

		}
	}
	return 0;
}
