#include <iostream>

using namespace std;

enum class Color { Red, Green, Yellow };

enum Date { Monday, Tuesday, Wednesday };

int main() {
  Color c = Color::Red;
  Date d = Date::Tuesday;
  return 0;
}
